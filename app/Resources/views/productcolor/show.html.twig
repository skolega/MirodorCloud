{% extends 'base.html.twig' %}

{% block body %}
    <h1>ProductColor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productColor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ productColor.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('productcolor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('productcolor_edit', { 'id': productColor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
